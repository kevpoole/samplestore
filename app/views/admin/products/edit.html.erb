<% content_for :title, "Editing product" %>

<div class="md:w-2/3 w-full p-4">
  <h1 class="font-bold text-3xl">Editing <%= @admin_product.name %></h1>

  <%= render "form", admin_product: @admin_product %>
  
</div>
