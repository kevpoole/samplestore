<div class="flex flex-col">

<div class="w-2/3 flex justify-between">  
  <h1 class="text-3xl font-bold mb-4">Background Image</h1>
  <% if @admin_background_image&.image&.attached? %>
     <%= link_to "Change Background Image", new_admin_background_image_path, class: "bg-blue-600 text-white h-fit px-4 py-2 rounded hover:bg-blue-500" %>
</div>
  <%= render "admin/background_images/background_image" %>
 
<% else %>
  <%= link_to "Add Image", new_admin_background_image_path, class: "bg-blue-600 text-white px-4 py-2 h-fit rounded" %>
<% end %>
</div>
