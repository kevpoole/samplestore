<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bandpage" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">


    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex min-h-screen">
        <nav class="w-fit ">
            <div class="w-64 h-screen bg-gray-400 sticky top-0">
                <div class="flex w-full">
                    <ul class="flex flex-col gap-4 p-4 w-full">
                        <li class="flex">
                            <%=link_to "View Site", "/", class: "w-full bg-gray-200 hover:bg-gray-600 hover:text-white rounded px-4 py-2 text-xl " %>
                        </li>
                        <li class="flex">
                            <%= link_to "Products", admin_products_path, class: "w-full rounded px-4 py-2 text-xl  #{current_page?(admin_products_path) ? 'bg-gray-600 text-white' : 'bg-gray-200 hover:bg-gray-600 hover:text-white'}" %>
                        </li>
                        <li class="flex">
                            <%=link_to "Product Categories", admin_categories_path, class: "w-full rounded px-4 py-2 text-xl  #{current_page?(admin_categories_path) ? 'bg-gray-600 text-white' : 'bg-gray-200 hover:bg-gray-600 hover:text-white'}" %>
                        </li>
                        <li class="flex">
                            <%=link_to "Shows", admin_shows_path, class: "w-full rounded px-4 py-2 text-xl  #{current_page?(admin_shows_path) ? 'bg-gray-600 text-white' : 'bg-gray-200 hover:bg-gray-600 hover:text-white'}" %>
                        </li>
                        <li class="flex">
                            <%=link_to "Links", admin_links_path, class: "w-full rounded px-4 py-2 text-xl  #{current_page?(admin_links_path) ? 'bg-gray-600 text-white' : 'bg-gray-200 hover:bg-gray-600 hover:text-white'}" %>
                        </li>
                        <li class="flex">
                            <%=link_to "Background Image", admin_background_images_path, class: "w-full rounded px-4 py-2 text-xl  #{current_page?(admin_background_images_path) ? 'bg-gray-600 text-white' : 'bg-gray-200 hover:bg-gray-600 hover:text-white'}" %>
                        </li>
                        <li class="flex">
                            <%=link_to "Sign Out", destroy_admin_session_path, data: {"turbo-method": :delete, "turbo-confirm": "Sign out?"}, class: "w-full bg-gray-200 hover:bg-gray-600 hover:text-white rounded px-4 py-2 text-xl " %>
                        </li>
                        
                        
                    </ul>
                </div>
                
            </div>

        
        </nav>
      <%= yield %>
    </main>
  </body>
</html>
