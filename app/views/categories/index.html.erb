<div class="flex flex-col mx-12">
  <div id="products" class="min-w-full flex flex-col md:grid md:grid-cols-2 gap-8 mt-12">
    <% @categories.each do |category| %>
      <%= render category %>
    <% end %>
  </div>
</div>